import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  sectionHeader: {
    paddingBottom: 10,
  },
  rowItem: {
    paddingBottom: 10,
  },
  rowItemTitle: {
    paddingTop: 5,
  },
});

export default styles;
