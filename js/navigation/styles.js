import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  linearGradient: {
    flex: 1,
  },
  titleIcon: {
    paddingRight: 10,
  },
  titleText: {
    fontSize: 22,
  },
});

export default styles;
